# Optional Python/NeMo runtime for NVIDIA Nemotron 3.5 ASR Streaming 0.6B.
# EchoMemory's in-app GGUF path uses NVIDIA NeMo-Speech.cpp (`nemo-speech`)
# instead; install that native runtime with the command in MODEL_SETUP.md.
# Requires Python 3.11+ and a compatible PyTorch CPU/CUDA installation.
Cython>=3.0
packaging>=24.0
torch>=2.7
transformers>=4.48
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@main#egg=nemo_toolkit[asr]

# For the downloaded GGUF model, the recommended lightweight runtime is
# NVIDIA NeMo-Speech.cpp (nemo-speech), which is a native executable rather
# than a pip dependency.
